# Repository instructions

- Use Node.js built-ins only; do not add dependencies.
- Run `npm test` after behavior changes.
- Keep public function signatures stable unless the task explicitly changes them.
- Do not modify `src/legacy.mjs`; it represents an unrelated compatibility surface.
