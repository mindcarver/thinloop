# Repository instructions

- Use browser APIs and Node.js built-ins only.
- Keep visible copy in `src/view.mjs`.
- Preserve current analytics event names.
- Run `npm test` after implementation changes.
