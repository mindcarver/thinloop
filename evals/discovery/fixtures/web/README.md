# Focusboard Web

Focusboard is a personal browser-based task board.

- Tasks and preferences are stored in local browser storage.
- The empty state includes a heading, explanation, and create button.
- The app has no calendar provider, model integration, team account, or server.
- Analytics records `empty_state_viewed` and `task_created`.
