# Repository instructions

- Use Node.js built-ins only.
- Keep response envelopes and public route shapes backward compatible.
- Treat billing and deletion callbacks as untrusted external input.
- Run `npm test` after implementation changes.
