# Taskline CLI

Taskline is a local-first personal task CLI.

- `tasks list` reads tasks from the local JSON store.
- List output defaults to 20 records and accepts an explicit `--limit`.
- Output is sorted by task id and contains id, title, and completion state.
- The repository has no remote service, account system, or team concept.
