# Repository instructions

- Use Node.js built-ins only.
- Preserve current CLI command names and exit-code conventions.
- Do not change storage format unless a request explicitly requires it.
- Run `npm test` after implementation changes.
